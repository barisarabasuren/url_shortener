import {Button} from 'antd'

const LandingPage = () => {
    return(
        <div>
            <p>Do you want your URLs to be muuuch(!) shorter? Give it a try!</p>
            <Button type="primary" shape="circle" size='large'>
                Get Started
            </Button>
        </div>
    );
};

export default LandingPage;